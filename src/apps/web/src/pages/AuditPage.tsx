export default function AuditPage() {
  return (
    <section className="panel">
      <h1>Audit</h1>
      <p className="lede">Global audit browser placeholder. Per-case audit API already exists.</p>
      <p className="note">
        Use <code>GET /api/v1/cases/&#123;id&#125;/audit</code> — full UI on Day 04+.
      </p>
    </section>
  );
}
